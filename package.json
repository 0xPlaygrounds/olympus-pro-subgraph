{
  "name": "olympus-pro",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "test": "graph test",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ 0xplaygrounds/olympus-pro-subgraph",
    "create-local": "graph create --node http://localhost:8020/ 0xplaygrounds/olympus-pro-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ 0xplaygrounds/olympus-pro-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 0xplaygrounds/olympus-pro-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.26.0",
    "@graphprotocol/graph-ts": "0.24.1",
    "matchstick-as": "^0.3.0"
  }
}
